/**
 * НАЗНАЧЕНИЕ: Глобальный клиентский стейт для каталога.
 * ЗАВИСИМОСТИ: Zustand.
 * ОСОБЕННОСТИ: В текущей версии используется только для дополнительных UI-фильтров, если это необходимо.
 */

import { create } from 'zustand';

interface WinesState {
    // В будущем здесь могут быть клиентские фильтры или временные состояния
}

/**
 * Хук-хранилище для клиентского состояния каталога.
 */
export const useWinesStore = create<WinesState>((set) => ({
    // Состояние пустое, так как данные теперь управляются TanStack Query
}));
