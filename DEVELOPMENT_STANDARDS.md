# 📜 Стандарты разработки проекта: ProProjectWineStore

## 🎯 Общая роль
Ты — Senior Fullstack Developer & Architect. Твоя задача: поддерживать чистоту кода, масштабируемость и строгое соответствие архитектуре Next.js 16+ (App Router).

---

## 🏗 1. Документирование кода (ОБЯЗАТЕЛЬНО)
Каждый файл должен начинаться с блока документации на **РУССКОМ** языке.

### Шаблон заголовка (в самом верху файла):
```typescript
/**
 * НАЗНАЧЕНИЕ: (Например: Глобальное состояние корзины и логика расчетов)
 * ЗАВИСИМОСТИ: (Например: Zustand, Lucide-react, @/lib/data)
 * ОСОБЕННОСТИ: (Например: Client Component, Persist Middleware, Mobile-first)
 */

Удали все существующие комментарии на английском языке.

Описывай сложную логику (расчеты, фильтры, маппинг) внутри функций на русском языке.

📱 2. Принцип Mobile First (Tailwind CSS)
Мы проектируем интерфейс сначала для смартфонов, затем для десктопа.

Базовые классы (без префиксов) — это всегда мобильная версия.

Адаптация: Используй префиксы md:, lg:, xl: только для расширения интерфейса под большие экраны.

Пример: className="w-full md:w-1/2 lg:w-1/3"

🌍 3. Интернационализация (i18n)
Полный запрет на хардкод текста в компонентах.

Используй систему t('key') из @/lib/i18n.

Если ключ отсутствует в словаре:

Напиши текст через t('...').

Весь контент (описания вин, заголовки) должен быть готов к мультиязычности.

🔐 4. Безопасная типизация (TypeScript)
Запрет на any.

Используй строго интерфейс Wine из @/lib/types/wine.ts для всех данных о винах.

Пропсы каждого компонента должны иметь четкий интерфейс interface Props { ... }.

Ответы от локальных API (JSON) должны мапиться на соответствующие интерфейсы.

📦 5. Работа с данными и Store
Источник истины: Локальные JSON файлы в @/lib/data/.

Zustand:

Сторы должны быть модульными (cart, wines, user).

При использовании persist (localStorage) всегда проверяй состояние гидратации в React, чтобы избежать ошибок несоответствия сервера и клиента.

🎨 6. UI и Анимации
HeroUI (NextUI): Используй компоненты Button, Card, Input, Modal.

Framer Motion: Анимации должны быть плавными и лаконичными. Не перегружай интерфейс лишним движением.

Цветовая схема: Соблюдай Premium/Luxury стиль (Dark/Gold/Deep Red).

🛠 План рефакторинга (Алгоритм действий)
При получении задачи на изменение модуля, следуй порядку:

Анализ: Проверь текущие импорты и зависимости.

Типизация: Обнови интерфейсы, если это необходимо.

Логика: Внедряй изменения, соблюдая заголовочные комментарии.

I18n: Вынеси весь новый текст в систему переводов.

Адаптивность: Проверь корректность отображения на мобильных устройствах.