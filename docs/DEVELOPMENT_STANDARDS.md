# 📜 Стандарты разработки проекта: ProProjectWineStore
DEVELOPMENT_STANDARDS.md

## 🎯 Общая роль
Ты — Senior Fullstack Developer & Architect. Твоя задача: поддерживать чистоту кода, масштабируемость и строгое соответствие архитектуре Next.js 16+ (App Router).

---

## 🏗 1. Документирование кода (ОБЯЗАТЕЛЬНО)
Каждый файл должен начинаться с блока документации на **РУССКОМ** языке.

### Шаблон заголовка (в самом верху файла):
```typescript
/**
 * НАЗНАЧЕНИЕ: (Например: Глобальное состояние корзины и логика расчетов)
 * ЗАВИСИМОСТИ: (Например: Zustand, Lucide-react, @/lib/data)
 * ОСОБЕННОСТИ: (Например: Client Component, Persist Middleware, Mobile-first)
 */
```

*   Удали все существующие комментарии на английском языке.
*   Описывай сложную логику (расчеты, фильтры, маппинг) внутри функций на русском языке.

---

## 📱 2. Принцип Mobile First (Tailwind CSS)
Мы проектируем интерфейс сначала для смартфонов, затем для десктопа.
*   **Базовые классы** (без префиксов) — это всегда мобильная версия.
*   **Адаптация**: Используй префиксы `md:`, `lg:`, `xl:` только для расширения интерфейса под большие экраны.
*   *Пример*: `className="w-full md:w-1/2 lg:w-1/3"`

---

## 🌍 3. Интернационализация (i18n)
Полный запрет на хардкод текста в компонентах.
1.  Используй хук `useTranslation` и метод `t('key')` из `@/lib/i18n`.
2.  Если ключ отсутствует в словаре:
    *   Добавь его в `lib/i18n.tsx`.
3.  Весь контент (описания вин, заголовки, кнопки) должен быть готов к мультиязычности (DE/EN).

---

## 🔐 4. Безопасная типизация (TypeScript)
**Запрет на `any`.**
1.  Используй строгие интерфейсы: `Wine` из `@/lib/types/wine.ts`, `Event` из `@/lib/types/event.ts`.
2.  Пропсы каждого компонента должны иметь четкий интерфейс `interface Props { ... }`.
3.  Ответы от API должны мапиться на соответствующие интерфейсы.

---

## 📦 5. Работа с данными и API (TanStack Query)
**Источник истины:** Удаленный сервер (или его эмуляция в `lib/api`).

1.  **TanStack Query (React Query)** — единственный способ получения серверных данных в компонентах.
    *   ✅ `const { data } = useWines();`
    *   ❌ `import products from '@/lib/data/products.json';` (Запрещено в компонентах!)
2.  **Слой API (`lib/api/`)**:
    *   Вся логика запросов (fetch, axios, mock timers) должна находиться здесь.
    *   Компоненты не должны знать, откуда приходят данные (JSON или реальный REST API).
3.  **Zustand**:
    *   Используется **только** для клиентского состояния (Корзина, Открытые модалки, Фильтры UI).
    *   Не дублируй серверные данные в Zustand, если они уже есть в React Query кэше.

---

## ☕️ 6. Backend & Architecture (Future Proof)
Мы готовим проект к миграции на **Spring Boot + PostgreSQL**.
1.  **Strict Contracts**: Интерфейсы TypeScript должны соответствовать будущим DTO Java-бэкенда.
2.  **Mocking**: Функции в `lib/api` сейчас эмулируют задержку сети (`delay(800)`), чтобы мы могли тестировать Loading States (Скелетоны).
3.  **Auth**: В будущем JWT будет храниться в HTTP-Only cookies. Сейчас имитируем токен в localStorage (для демо).

---

## 🎨 7. UI и Анимации
1.  **HeroUI (NextUI)**: Используй стандартные компоненты (`Button`, `Card`, `Input`, `Modal`) для консистентности.
2.  **Framer Motion**: Анимации должны быть плавными ("Premium Feel"). Не перегружай интерфейс.
3.  **Цветовая схема**: Соблюдай стилистику бренда (Dark Zinc, Gold, Deep Red).

---

## 🤖 8. AI Guidelines
Новый раздел для AI-фич.
1.  **Streaming**: Ответы AI должны приходить потоком (Stream), чтобы не заставлять пользователя ждать.
2.  **Context**: При отправке запроса в AI, всегда обогащай его контекстом (текущая страница, выбранное вино).

---

## 🍪 9. Cookie Consent & GDPR Compliance (MVP)
Европейский рынок требует строгого соблюдения GDPR.
1.  **localStorage Preference**: Сохраняй выбор пользователя ("Necessary", "Analytics", "Marketing") в `localStorage` (ключ `cookie_consent_preferences`).
2.  **Блокировка скриптов**:
    *   Аналитика (GA4, Pixel) должна загружаться только если `analytics: true`.
    *   Маркетинг-скрипты только если `marketing: true`.
3.  **UI**: Баннер должен быть ненавязчивым, но заметным (Fixed Bottom), с возможностью детальной настройки.

---

## 🛠 План рефакторинга (Алгоритм действий)
При получении задачи на изменение модуля, следуй порядку:
1.  **Анализ**: Проверь текущие импорты. Убедись, что нет прямых импортов JSON.
2.  **Типизация**: Обнови интерфейсы, если данные изменились.
3.  **Логика**: Внедряй изменения, обновляя API слой и Хуки.
4.  **UI/I18n**: Обнови верстку и вынеси тексты в переводы.
5.  **Mobile Check**: Проверь отображение на ширине 375px.